# Dependencies (reinstalled inside image)
node_modules
apps/backend/node_modules
apps/frontend/node_modules

# Build outputs
apps/backend/dist
apps/frontend/.next

# WhatsApp session files (now stored in DB — files are legacy)
apps/backend/sessions

# Git & IDE
.git
.gitignore
.vscode
.idea

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Environment files (injected at runtime via docker-compose or platform)
.env
apps/backend/.env
apps/frontend/.env.local

# Docs (not needed in image)
*.md
